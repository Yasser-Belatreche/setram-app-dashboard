import React from 'react';

import { Layout } from '../../components/Layout/Layout';

const Documents: React.FC = () => {
    return (
        <Layout>
            <h1>Documents</h1>
        </Layout>
    );
};

export { Documents };
