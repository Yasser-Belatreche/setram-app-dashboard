import React from 'react';

import { Layout } from '../../components/Layout/Layout';

const Events: React.FC = () => {
    return (
        <Layout>
            <h1>Events</h1>
        </Layout>
    );
};

export { Events };
