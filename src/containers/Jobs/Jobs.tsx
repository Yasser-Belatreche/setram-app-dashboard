import React from 'react';

import { Layout } from '../../components/Layout/Layout';

const Jobs: React.FC = () => {
    return (
        <Layout>
            <h1>Jobs</h1>
        </Layout>
    );
};

export { Jobs };
