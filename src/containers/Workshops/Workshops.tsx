import React from 'react';

import { Layout } from '../../components/Layout/Layout';

const Workshops: React.FC = () => {
    return (
        <Layout>
            <h1>Workshops</h1>
        </Layout>
    );
};

export { Workshops };
